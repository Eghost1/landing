# .htaccess para optimización SEO de Nubira

# Habilitar compresión GZIP
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>

# Cache de archivos estáticos
<IfModule mod_expires.c>
    ExpiresActive on
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType application/pdf "access plus 1 month"
    ExpiresByType text/html "access plus 1 hour"
</IfModule>

# ==================================================
# REDIRECCIONES SEO Y DEEP LINKS
# ==================================================
RewriteEngine On

# ==================================================
# UNIVERSAL LINKS - Mantener acceso directo a .well-known
# ==================================================
# Asegurar que .well-known sea accesible (debe ir PRIMERO)
RewriteRule ^\.well-known/ - [L]

# ==================================================
# FORZAR HTTPS Y WWW
# ==================================================
# Forzar HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Forzar WWW (opcional - ajustar según preferencia)
RewriteCond %{HTTP_HOST} ^nubiraoficial\.cl [NC]
RewriteRule ^(.*)$ https://www.nubiraoficial.cl/$1 [L,R=301]

# ==================================================
# DEEP LINKS REDIRECTION FIX
# ==================================================
# Redirigir /servicio/* a /pages/servicio/*

# Redirigir servicio/ y servicio/index.html a pages/servicio/
RewriteRule ^servicio/?$ /pages/servicio/ [R=301,L]
RewriteRule ^servicio/index\.html$ /pages/servicio/index.html [R=301,L]

# Redirigir servicio/{id}.html a pages/servicio/{id}.html
RewriteRule ^servicio/([0-9]+)\.html$ /pages/servicio/$1.html [R=301,L]

# Redirigir servicio/{id} (sin extensión) a pages/servicio/{id}
RewriteRule ^servicio/([0-9]+)$ /pages/servicio/$1 [R=301,L]

# Redirigir trabajador/ y trabajador/index.html a pages/trabajador/
RewriteRule ^trabajador/?$ /pages/trabajador/ [R=301,L]
RewriteRule ^trabajador/index\.html$ /pages/trabajador/index.html [R=301,L]

# Redirigir trabajador/{id}.html a pages/trabajador/{id}.html
RewriteRule ^trabajador/([0-9]+)\.html$ /pages/trabajador/$1.html [R=301,L]

# Redirigir trabajador/{id} (sin extensión) a pages/trabajador/{id}
RewriteRule ^trabajador/([0-9]+)$ /pages/trabajador/$1 [R=301,L]

# Remover extensión .html de las URLs
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Redireccionar URLs con .html a sin extensión
RewriteCond %{THE_REQUEST} /([^.]+)\.html [NC]
RewriteRule ^ /%1 [NC,L,R=301]

# Redirecciones específicas para SEO de Nubira
Redirect 301 /home /
Redirect 301 /inicio /
Redirect 301 /about /nosotros
Redirect 301 /team /nosotros
Redirect 301 /contact /
Redirect 301 /contacto /

# Headers de seguridad
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options nosniff
    Header always set X-Frame-Options DENY
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# ==================================================
# UNIVERSAL LINKS - MIME TYPES Y HEADERS
# ==================================================
# Configurar MIME types correctos para archivos de configuración
<IfModule mod_mime.c>
    # Apple App Site Association (sin extensión)
    <FilesMatch "^apple-app-site-association$">
        Header set Content-Type "application/json"
        Header set Access-Control-Allow-Origin "*"
    </FilesMatch>
    
    # Android Asset Links
    <FilesMatch "^assetlinks\.json$">
        Header set Content-Type "application/json"
        Header set Access-Control-Allow-Origin "*"
    </FilesMatch>
</IfModule>

# Permitir acceso a archivos .well-known
<IfModule mod_authz_core.c>
    <Directory "/\.well-known">
        Require all granted
    </Directory>
</IfModule>

# Bloquear acceso a archivos sensibles (excepto .well-known)
<FilesMatch "^\.(?!well-known)">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

<Files ~ "(\.htaccess|\.htpasswd|\.ini|\.log|\.sh|\.sql|\.bak)$">
    Order allow,deny
    Deny from all
</Files>

# Optimización de imágenes
<IfModule mod_headers.c>
    <FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
        Header set Cache-Control "max-age=31536000, public"
    </FilesMatch>
</IfModule>

# Error pages personalizadas (opcional)
ErrorDocument 404 /404.html
ErrorDocument 500 /500.html

# Prevenir hotlinking de imágenes
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} !^https?://(www\.)?nubiraoficial\.cl [NC]
RewriteRule \.(jpg|jpeg|png|gif|webp)$ - [F]
